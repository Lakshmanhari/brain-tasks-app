version: 0.2

env:
  variables:
    AWS_REGION: ap-south-1
    ECR_REPO: brain-tasks-app
    IMAGE_TAG: latest

phases:
  pre_build:
    commands:
      - echo Logging in to ECR
      - aws ecr get-login-password --region $AWS_REGION | docker login --username AWS --password-stdin 216989110867.dkr.ecr.ap-south-1.amazonaws.com

  build:
    commands:
      - echo Build Docker image
      - docker build -t $ECR_REPO:$IMAGE_TAG .

  post_build:
    commands:
      - echo Tag image
      - docker tag $ECR_REPO:$IMAGE_TAG 216989110867.dkr.ecr.ap-south-1.amazonaws.com/brain-tasks-app
      - echo Push image
      - docker push 216989110867.dkr.ecr.ap-south-1.amazonaws.com/brain-tasks-app

